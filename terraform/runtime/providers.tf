provider "tls" {
  # Default configuration — no credentials or endpoints required.
}

provider "random" {
  # Default configuration — no credentials or endpoints required.
}

provider "local" {
  # Default configuration — writes rendered artifacts to the local filesystem.
}
