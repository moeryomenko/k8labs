# k8sbr0.network — systemd-networkd network configuration for the cluster bridge
# DHCP is provided by systemd-networkd's built-in DHCP server.
# dnsmasq is DNS-only.
[Match]
Name=k8sbr0

[Network]
Address=192.168.124.1/24
IPForward=yes
DHCPServer=no
