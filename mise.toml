[tools]
# Tool versions are pinned for reproducibility. Update together when upgrading the cluster.
opentofu = "1.8.0"
packer = "1.11.2"
kubectl = "1.32.0"

[env]
# Use project-wide kubeconfig so kubectl works without --kubeconfig flag
KUBECONFIG = "{{config_root}}/kubeconfig"
