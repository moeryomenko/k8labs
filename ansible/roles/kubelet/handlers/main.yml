---
# kubelet — handlers
#
# Restart kubelet when its TLS material or config changes so a fresh CA /
# certificates / kubeconfig are loaded by the running daemon.

- name: restart kubelet
  ansible.builtin.systemd:
    name: kubelet
    state: restarted
    daemon_reload: true
