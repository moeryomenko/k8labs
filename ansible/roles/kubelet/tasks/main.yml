---
# kubelet — Configure and start kubelet on all cluster nodes
#
# Runs on both control-plane and worker nodes.
# Distributing per-node kubeconfig + certs, then starting kubelet.

- name: Create kubelet PKI directory
  ansible.builtin.file:
    path: /var/lib/kubelet/pki
    state: directory
    mode: "0755"

- name: Create kubelet data directory
  ansible.builtin.file:
    path: /var/lib/kubelet
    state: directory
    mode: "0755"

- name: Copy CA certificate
  ansible.builtin.copy:
    src: "{{ certs_dir }}/ca.pem"
    dest: /var/lib/kubelet/pki/ca.pem
    mode: "0644"

- name: Copy kubelet certificate
  ansible.builtin.copy:
    src: "{{ certs_dir }}/kubelets/{{ inventory_hostname }}.pem"
    dest: /var/lib/kubelet/pki/{{ inventory_hostname }}.pem
    mode: "0644"

- name: Copy kubelet key
  ansible.builtin.copy:
    src: "{{ certs_dir }}/kubelets/{{ inventory_hostname }}-key.pem"
    dest: /var/lib/kubelet/pki/{{ inventory_hostname }}-key.pem
    mode: "0600"

- name: Copy kubelet kubeconfig
  ansible.builtin.copy:
    src: "{{ certs_dir }}/kubelets/{{ inventory_hostname }}.kubeconfig"
    dest: /var/lib/kubelet/kubeconfig
    mode: "0600"

- name: Resolve {{NODE_IP}} placeholder in kubelet config
  ansible.builtin.replace:
    path: /etc/kubernetes/kubelet/config.yaml
    regexp: "{{ '{{NODE_IP}}' }}"
    replace: "{{ ansible_host }}"
  failed_when: false

- name: Start kubelet
  ansible.builtin.systemd:
    name: kubelet
    state: started
    enabled: true
