---
# etcd — handlers
#
# Restart etcd when its config file changes so the resolved {{CP_IP}}
# placeholder is picked up by the running daemon.

- name: restart etcd
  ansible.builtin.systemd:
    name: etcd
    state: restarted
    daemon_reload: true
