# k8s-w1.netdev — TAP device for worker node 1
#
# Creates a persistent TAP device that the first worker
# QEMU VM connects to. Must be enslaved to k8sbr0 via a
# .network file (see README).

[NetDev]
Name=k8s-w1
Kind=tap
