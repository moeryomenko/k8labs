# k8s-cp1.network — Enslave control-plane TAP device to k8sbr0 bridge
#
# This file must be copied alongside k8s-cp1.netdev so the TAP device
# is automatically attached to the bridge when systemd-networkd starts.

[Match]
Name=k8s-cp1

[Network]
Bridge=k8sbr0
